/**INTERFACE PARA INFORMACIÓN DE CONTACTO */
  export interface ContactInformation {
    id: string;
    info: string;
  }

/**INTERFACE PARA OPCIONES NAV */
 export   interface OptionsNav {
    id: string;
    ref: string;
    text: string;
  }