import { Cloudinary } from "@cloudinary/url-gen";

// Reemplaza "tu_cloud_name" con el nombre real de tu nube de Cloudinary
export const cld = new Cloudinary({ 
  cloud: { 
    cloudName: 'do7og4zuk' 
  } 
});